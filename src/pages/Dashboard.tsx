import { Box, Typography } from '@mui/material';

const Dashboard = () => (
  <Box p={3}>
    <Typography variant="h4" fontWeight={600}>
      <h1>lỗi nhiều quá fix lẹ đi</h1>
    </Typography>
  </Box>
);

export default Dashboard;
