import { Box, Typography } from '@mui/material';

const Dashboard = () => (
  <Box p={3}>
    <Typography variant="h4" fontWeight={600}>
      Code giao diẹn ở đây 
    </Typography>
  </Box>
);

export default Dashboard;
