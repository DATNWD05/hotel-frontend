import { Box, Typography } from '@mui/material';

const Dashboard = () => (
  <Box p={3}>
    <Typography variant="h4" fontWeight={600}>
      
    </Typography>
  </Box>
);

export default Dashboard;
